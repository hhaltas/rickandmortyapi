import styled from 'styled-components';

const Subtitle = styled.Text.attrs({
  numberOfLines: 1,
})`
  font-weight: 100;
  text-align: center;
  font-size: 14px;
  padding: 5px 0;
`;

export default Subtitle;